# Placeholder GraphQL schema for ESLint validation.
# Replace this with your generated schema once Pothos is set up.
# Run `pnpm generate:schema` (or equivalent) to populate this file.

schema {
  query: Query
  mutation: Mutation
}

type Query {
  health: Boolean
}

type Mutation {
  health: Boolean
}
